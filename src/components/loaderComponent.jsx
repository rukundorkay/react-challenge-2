import React, { Component } from 'react';
class Loader extends Component {
    //state = { loader:"" }
    render() {
        return (<p>loading.......</p>);
        }
    }

    export default Loader;